import 'package:flutter/material.dart';

class RestaurantFoodMenuPage extends StatelessWidget {
  const RestaurantFoodMenuPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      // appBar: AppBar(
      //   title: const Text('Edit Food Menu'),
      // ),
      body: Center(child: Text('Edit Food Menu')),
    );
  }
}
